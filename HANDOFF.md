# 引き継ぎメモ

**最終更新：2026-08-28**
**ブランチ：** `integration/email-and-media`　**最終コミット：** `8d7241d feat: 確認への具体的な回答・条件の上書きに対応する`

## 次の1手

**`docs/請求切替手順書.md` の第1部を実行する（Anthropic Console でクレジット購入）。**
これが全部のブロッカー。買った時点でAI秘書は自動復旧する（コード変更・デプロイ不要）。

## 続きのアクション

1. 請求切替 第1部（請求先情報→会社カード→残高清算→クレジット購入→上限/自動リロード/通知）… 下村さんの操作・15分・**無停止**
2. AI判定2つを検証 … `src/lib/clarify.ts` の `detectMissing` と `interpretAnswer` に実データを通す
3. デプロイ（`git push` ＋ Vercel）… **要承認**。現在 origin より2コミット ahead
4. 8/20〜8/27の取りこぼし拾い直し … 履歴ファイルは8/27まであるので後から拾える
5. （任意）第2部＝ワークスペース分離。やらなくても復旧はするが、巻き添え停止の構造は残る

## 今どうなっているか

- **動いている**：作業ツリーはクリーン。stashなし。ビルド・型チェック通過。
  ロジックのテストは全通過（承認判定20件・条件上書きの状態遷移5件）
- **作りかけ**：`src/lib/clarify.ts` の**AI判定2つが未検証**
  - `detectMissing` … 依頼から不足条件を検出する
  - `interpretAnswer` … 「回答」か「別の新規依頼」かを判別する
  どちらもAPIを叩かないと確認できない。残高復旧後に最優先で検証する
- **壊れている**：**Anthropic API が残高切れ**（2026-08-28 実測 `400 invalid_request_error`）。
  Notionの最新タスク登録は **2026-08-13 13:58 で止まっている**。
  お盆（8/13〜19）を除いても **8/20以降 約6営業日分の依頼が未記録**

## 決まっていないこと

- **第1部だけ先にやるか、第2部（ワークスペース分離）まで一気にやるか** … 判断：下村さん
  - A案：第1部だけ → 即復旧・無停止。ただし他キーの使いすぎで再び巻き添え停止する構造は残る
  - B案：第2部まで → 2〜3分停止するが、業務用と開発用の残高が分離され再発しない
- 継続課金（固定費）の社内承認、税理士へのインボイス確認 … 未実施（手順書 第3部）
- 「Seller report」キー（$2.24 / Opus 5 / 8月18〜19日に単発使用）の用途 … **不明のまま**。
  会社カードに切り替えるとこれも会社負担になる

## これまでの流れ

| 日付 | やったこと |
|---|---|
| 8/14 | メール送信・画像/PDF受信を本番系へ統合、ログインフォーム＋Cookie方式へ |
| 8/15 | 担当者の氏名正規化、物件名で串刺し、休業日カレンダー、休業明け棚卸しをCron化 |
| 8/22 | リマインド・棚卸しを打ち切らず全件表示（複数通に分割） |
| 8/27 | 初動確認（条件不足の依頼に提案付きで聞き返す）を実装 |
| 8/27 | 具体的な回答・条件の上書きに対応（「名義を仲介会社宛てに変えて」等） |
| 8/27 | `docs/請求切替手順書.md` 作成 |

## 注意点・ハマりどころ

- **APIキーをチャットに貼らない。** 第2部でキーを差し替えるときは、Vercelの管理画面で
  下村さんご自身が環境変数を書き換えるのが安全（会話ログに残さない）
- **旧APIキーは、新キーで動作確認できてから削除する。** 逆順にするとAI秘書が停止する
- **請求先情報（会社名・住所）はカード登録より先に入れる。**
  領収書は発行時点の情報で作られ、後から遡って直せない
- **初動確認はタスク登録の門番にしていない。** `createNotionTask` の**後**に走る設計。
  ここを前に動かすと、誰も答えなかった依頼が消える。順番を変えないこと
- `git push`・デプロイは必ず事前承認を取る
- ロジックのテストは `tmp/` にある（gitignore済み）：
  `npx tsx tmp/clarify.test.ts` / `npx tsx tmp/answer.test.ts`

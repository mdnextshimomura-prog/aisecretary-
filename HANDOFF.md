# 引き継ぎメモ

**最終更新：2026-08-28（初動確認の設計完了・push待ち）**
**ブランチ：** `integration/email-and-media`　**最終コミット：** `3bd9d26 feat: 確認項目を.mdで定義し、条件が揃ったら担当者へ引き渡す`

## 次の1手

**`git push origin integration/email-and-media` を実行する。**
5コミット分が未pushで、初動確認の機能はまだ本番に出ていない。
（Claude側から push を試みたが自動モードの制限で拒否された。手動実行が必要）

## 続きのアクション

1. push → Vercelデプロイ → LINEで実地確認（「これ買いたい」を投げて聞き返しが来るか）
2. 請求を**会社カードへ切り替える**（`docs/請求切替手順書.md` 第1部）
   … 8/28時点で下村さんの**個人カード**で立替済み。会社付替えが未処理
3. 運用しながら `docs/依頼チェックリスト.md` の項目を調整する
   （実務と合わない提案値が出たら、その表の1行を直して `npm run build:checklist`）
4. （任意）ワークスペース分離。巻き添え停止の構造は残ったまま

## 今どうなっているか

- **動いている**：作業ツリーはクリーン。stashなし。ビルド・型チェック通過。
  ロジックのテストは全通過（承認判定20件・条件上書きの状態遷移5件）
- **検証済み**：`src/lib/clarify.ts` のAI判定2つを実データで確認（8/28）。全ケース通過
  - `detectMissing` … 「これ買いたい」→ 買主名義・金額を不足として検出。条件が揃った依頼では聞き返さない
  - `interpretAnswer` … 部分回答・1語回答を反映。**確認中に来た別依頼は吸い込まない**（最重要ケース）
  - 検証中に欠陥を2つ発見して修正済み（`eff8719`）：宛先の項目欠落、修正指示の取りこぼし
- **復旧済み**：Anthropic API は 8/28 に下村さんの個人カードで残高を補充し復旧（実測 HTTP 200）
- **登録済み**：8/20〜8/27の取りこぼし30件をNotionへ遡り登録（8/28）。
  期日は受信日基準で計算。連名の担当は先頭のみをselectに入れ、全員はページ本文に記載
- **確認済み**：Notion側で完了/未着手/要確認に変えるとリマインドに反映される（実測）
- **未pushの機能**：初動確認・担当者への引き渡し。`git push` するまで本番では動かない

## 決まっていないこと

- **請求を会社カードへ切り替えるか** … 現状は個人カードで立替中
- 「Seller report」キー（$2.24 / Opus 5 / 8月18〜19日）の用途 … **不明のまま**
- 継続課金（固定費）の社内承認、税理士へのインボイス確認 … 未実施（手順書 第3部）
- 「Seller report」キー（$2.24 / Opus 5 / 8月18〜19日に単発使用）の用途 … **不明のまま**。
  会社カードに切り替えるとこれも会社負担になる

## これまでの流れ

| 日付 | やったこと |
|---|---|
| 8/14 | メール送信・画像/PDF受信を本番系へ統合、ログインフォーム＋Cookie方式へ |
| 8/15 | 担当者の氏名正規化、物件名で串刺し、休業日カレンダー、休業明け棚卸しをCron化 |
| 8/22 | リマインド・棚卸しを打ち切らず全件表示（複数通に分割） |
| 8/27 | 初動確認（条件不足の依頼に提案付きで聞き返す）を実装 |
| 8/27 | 具体的な回答・条件の上書きに対応（「名義を仲介会社宛てに変えて」等） |
| 8/27 | `docs/請求切替手順書.md` 作成 |
| 8/28 | 残高復旧（個人カード）。AI判定を実データ検証し、欠陥2件を修正 |
| 8/28 | 8/20〜27の取りこぼし30件をNotionへ遡り登録 |
| 8/28 | 確認項目を`docs/依頼チェックリスト.md`に外出し。自動補完・担当者への引き渡しを追加 |

## 注意点・ハマりどころ

- **APIキーをチャットに貼らない。** 第2部でキーを差し替えるときは、Vercelの管理画面で
  下村さんご自身が環境変数を書き換えるのが安全（会話ログに残さない）
- **旧APIキーは、新キーで動作確認できてから削除する。** 逆順にするとAI秘書が停止する
- **請求先情報（会社名・住所）はカード登録より先に入れる。**
  領収書は発行時点の情報で作られ、後から遡って直せない
- **初動確認はタスク登録の門番にしていない。** `createNotionTask` の**後**に走る設計。
  ここを前に動かすと、誰も答えなかった依頼が消える。順番を変えないこと
- `git push`・デプロイは必ず事前承認を取る
- ロジックのテストは `tmp/` にある（gitignore済み）：
  `npx tsx tmp/clarify.test.ts` / `npx tsx tmp/answer.test.ts` / `npx tsx tmp/ai.test.ts`（API課金あり）
- **遡り登録の期日に注意。** `resolveDue` は「今日」基準で計算するため、
  8/20の依頼をそのまま登録すると期日が今日起算になる。登録時は受信日を渡すこと
- **LINEのテキスト書き出しには画像・PDFの中身が入らない。**
  添付だけの依頼は履歴からは復元できない（8/20以降で2件）
- **確認項目を足すときは `docs/依頼チェックリスト.md` を編集する。**
  `src/lib/checklist.generated.ts` は自動生成なので直接編集しない。
  編集後は `npm run build:checklist`（`npm run build` にも組み込み済み）
- **項目のキーは一度決めたら変えない。** 変えると確認中のタスクが繋がらなくなる
- **必須項目は推測で埋めない設計。** プロンプトで明示的に禁止している。
  ここを緩めると「査定お願い」から前提を勝手に断定して事故る
